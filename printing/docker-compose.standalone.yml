services:
  print-server:
    build:
      context: .
      dockerfile: Dockerfile.https
    image: posr-print-https:latest
    ports:
      - "3132:443"
    volumes:
      - ./certs:/certs:ro
    restart: unless-stopped
